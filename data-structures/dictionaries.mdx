---
title: Dictionaries
description: Store data with key-value pairs
icon: "book"
---

## What are dictionaries?

Dictionaries store data in key-value pairs. Think of them like a real dictionary where you look up a word (key) to find its definition (value).

Real-world examples:
- Phone book: name > phone number
- Menu: dish > price
- User profile: username > user info

## Creating Dictionaries

Python provides several ways to create dictionaries depending on your data source and style:

### 1. Using Literal Curly Braces `{}`
The most common way to create a dictionary is using curly braces with key-value pairs separated by colons:

```python
# Empty dictionary
my_dict = {}

# With initial data
person = {
    "name": "Aarav",
    "age": 30,
    "city": "Mumbai"
}
```

### 2. Using the `dict()` Constructor
You can construct dictionaries using the built-in `dict()` constructor in two ways:

* **Keyword arguments**: Keys are passed as keyword arguments (keys are automatically converted to strings):
  ```python
  scores = dict(math=95, english=87, science=92)
  ```
* **List of key-value tuples**: Useful when converting zipped or tabular sequences into a dictionary:
  ```python
  pairs = [("math", 95), ("english", 87), ("science", 92)]
  scores = dict(pairs)
  ```

### 3. Using `dict.fromkeys()`
Useful when you want to initialize a dictionary with a set of keys mapping to the same default value:

```python
keys = ["math", "english", "science"]
scores = dict.fromkeys(keys, 0)  # {'math': 0, 'english': 0, 'science': 0}
```

### 4. Dictionary Comprehension
A concise way to construct dictionaries dynamically using loops:

```python
# Create a dictionary of squares: {1: 1, 2: 4, 3: 9}
squares = {x: x**2 for x in range(1, 4)}
```

<Note>
  Keys must be unique and of an **immutable** type (like strings, numbers, or tuples). Values can be of any data type and can repeat.
</Note>

## Accessing values

```python
person = {"name": "Alice", "age": 30, "city": "New York"}

# Get values by key
print(person["name"])       # "Alice"
print(person["age"])        # 30

# Safer with get()
print(person.get("job"))    # None (no error)
print(person.get("job", "Unknown"))  # "Unknown" (default)
```

## Changing dictionaries

```python
person = {"name": "Alice", "age": 30}

# Add or update
person["email"] = "alice@email.com"  # Add new
person["age"] = 31                   # Update existing

# Remove items
del person["email"]              # Remove by key
age = person.pop("age")          # Remove and return
person.clear()                   # Remove all items
```

## Dictionary methods

```python
person = {"name": "Alice", "age": 30, "city": "New York"}

# Get all keys, values, or items
print(person.keys())    # dict_keys(['name', 'age', 'city'])
print(person.values())  # dict_values(['Alice', 30, 'New York'])
print(person.items())   # dict_items([('name', 'Alice'), ...])

# Check if key exists
if "name" in person:
    print("Name found!")

# Update multiple values
person.update({"age": 31, "job": "Engineer"})
```

### Nested dictionaries
```python
# Dictionary of dictionaries
students = {
    "alice": {"age": 20, "grade": "A"},
    "bob": {"age": 21, "grade": "B"},
    "charlie": {"age": 19, "grade": "A"}
}

# Access nested data
print(students["alice"]["grade"])  # "A"
```

## Common mistakes

<AccordionGroup>
  <Accordion title="KeyError when key doesn't exist">
    ```python
    # Wrong
    person = {"name": "Alice"}
    print(person["age"])  # KeyError!
    
    # Right - use get()
    print(person.get("age", 0))  # Returns 0 if missing
    ```
  </Accordion>

  <Accordion title="Using mutable keys">
    ```python
    # Wrong - lists can't be keys
    bad_dict = {[1, 2]: "value"}  # TypeError!
    
    # Right - use immutable types
    good_dict = {(1, 2): "value"}  # Tuple is OK
    good_dict = {"1,2": "value"}   # String is OK
    ```
  </Accordion>
  
</AccordionGroup>

## Practice & Exercises

To reinforce what you've learned in this section (Dictionaries, creation patterns, key accesses, and updates), practice with these interactive notebooks:

<CardGroup cols={2}>
  <Card
    title="Follow-Along Practice"
    icon="laptop-code"
  >
    Practice initializing dictionaries in various ways, looking up values, and editing key-value pairs.

    [💻 VS Code](vscode://file/Users/sivaprasad/Downloads/python%20material/python4ai/public/notebooks/basics_exercises/Dictionaries_Practice.ipynb) | [🚀 Colab](https://colab.research.google.com/github/prasad230776/python4ai/blob/master/public/notebooks/basics_exercises/Dictionaries_Practice.ipynb) | <a href="/public/notebooks/basics_exercises/Dictionaries_Practice.ipynb" download>📥 Download</a>
  </Card>
  <Card
    title="Practice Exercises"
    icon="pen-to-square"
  >
    Test your knowledge with hands-on exercises on school grades databases and safe lookups.

    [💻 VS Code](vscode://file/Users/sivaprasad/Downloads/python%20material/python4ai/public/notebooks/basics_exercises/Dictionaries_Exercises.ipynb) | [🚀 Colab](https://colab.research.google.com/github/prasad230776/python4ai/blob/master/public/notebooks/basics_exercises/Dictionaries_Exercises.ipynb) | <a href="/public/notebooks/basics_exercises/Dictionaries_Exercises.ipynb" download>📥 Download</a>
  </Card>
</CardGroup>

## What's next?

Finally, let's explore Sets - collections of unique items!

<Card
  title="Sets"
  icon="circle-nodes"
  href="/data-structures/sets"
>
  Learn about unique values
</Card>